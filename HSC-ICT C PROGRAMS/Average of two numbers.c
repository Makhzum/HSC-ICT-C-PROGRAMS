/*Average of two numbers*/

#include <stdio.h>

    main()
{
    float A, B, AVG;
    printf("Enter the value of A:");
    scanf("%f", &A);
    printf("Enter the value of B:");
    scanf("%f", &B);
    AVG=(A+B)/2;
    printf("Summation is %d", AVG);
    return 0;
}
